on: 
  push:

name: CI build
jobs:
  build:
    name: CI build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: ^1.20

    - name: Build binaries
      run: go build -o dnscontrol-extended -ldflags "-s -w"
      env:
        CGO_ENABLED: 0